@model IEnumerable<WebApplication1.DAO.Report>

@{
    ViewBag.Title = "Reports";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="form-horizontal">
    <br />
    <br />
<h2>Отчеты</h2>
<p>
    @{
        if (User.IsInRole("School_Stuff")) { 
        Html.ActionLink("Создать", "Create");
        }
      }
          </p>
<table class="table">
    <tr>
        <th>
            Название
        </th>
        <th>
            Дата
        </th>
        <th>
            Статус отчета
        </th>
        <th>
            Тип отчета
        </th>
        <th>
            Наименование МОУ
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Where(x => x.Status.Id == 2)) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status.Status1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School.Name)
        </td>
        <td>
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
              @Html.ActionLink("Проверить", "Allow", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

@foreach (var item in Model.Where(x => x.Status.Id == 3))
{
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Date)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Status.Status1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Type.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.School.Name)
    </td>
    <td></td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
        @Html.ActionLink("Проверить", "Allow", new { id=item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
    </td>
</tr>
}

@foreach (var item in Model.Where(x => x.Status.Id == 1))
{
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Date)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Status.Status1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Type.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.School.Name)
    </td>
    <td></td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
        @Html.ActionLink("Проверить", "Allow", new { id=item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
    </td>
</tr>
}

</table>
   </div>

<script>

</script>